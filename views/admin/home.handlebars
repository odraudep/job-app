{{#each users}}
  <p>Id: {{_id}}</p>
  <p>Name: {{name}}</p>
  <p>E-mail: {{email}}</p>
  <p>IsAdm: {{isAdm}}</p>
  <p style="word-wrap: break-word;">Password: {{password}}</p>

  <form action="/admin/users/delete/{{_id}}" method="POST">
    <a href="/admin/users/delete/{{_id}}">
      <button class="btn btn-outline-danger">Delete</button>
    </a>
  </form>

  <hr>
{{/each}}
